export type TextTitleProps = {
  title: string;
  color?: string;
  description?: string;
};

export type DescriptionTextProps = {
  description: string;
};

export type ServicesProps = {
  title: string;
  description: string;
  icon: string;
  abbr?: string;
};
